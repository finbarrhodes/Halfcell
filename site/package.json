{
  "name": "halfcell-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "observable preview",
    "build": "observable build && node scripts/copy-static.mjs",
    "clean": "node -e \"require('fs').rmSync('src/.observablehq/cache', {recursive: true, force: true})\"",
    "preview:dist": "npm run build && node scripts/serve-dist.mjs"
  },
  "dependencies": {
    "@observablehq/framework": "^1.13.4"
  }
}
