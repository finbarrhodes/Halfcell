# Development and test dependencies. Install alongside requirements.txt:
#     pip install -r requirements.txt -r requirements-dev.txt
#
# The runtime requirements deliberately exclude these so the refresh and deploy
# workflows install only what the pipeline needs.

pytest>=8.0.0
pytest-cov>=5.0.0
