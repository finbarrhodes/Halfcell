# halfcell

## Workflow Rules

- After any change to methodology logic or calculations, review `src/visualization/methodology.py` and update it to reflect the change before finishing.

- After completing any change that originated from a task in `TODOS.md`, revisit `TODOS.md` and update that entry: delete it if fully complete, or edit it down / partially strike it through if the task is only partially complete (intentionally or otherwise).
